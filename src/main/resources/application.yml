server:
  #端口
  port: 8099
  #Spring相关配置
Spring:
  #数据库配置
  datasource:
    driverClassName: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/novel
    username: root
    password: 1qa2ws3ed
   #mybatis相关配置
mybatis-plus:
  configuration:
    #控制台打印完整带参数SQL语句
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
    map-underscore-to-camel-case: true
    type-aliases-package: top.novel.entity
#sql日志
logging:
  level:
    com.novel.mapper: debug